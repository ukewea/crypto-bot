# API Server Configuration
# Set this to change the backend API port (both frontend and backend will use this port)
# VITE_API_PORT=39583

# Alternative: Set PORT for backend only (frontend will need VITE_API_PORT to match)
# PORT=39583